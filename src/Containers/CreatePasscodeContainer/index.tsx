import React from 'react';
import {View, Text} from 'react-native';
import CreatePasscode from './CreatePasscode';

const CreatePasscodeContainer: React.FC = () => {
  return <CreatePasscode />;
};

export default CreatePasscodeContainer;
